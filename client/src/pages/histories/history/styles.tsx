import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  header: {
    marginBottom: "50px"
  },
  headerButtons: {
    marginBottom: "32px"
  },
  headerTitle: {
    marginBottom: "16px"
  }
});
